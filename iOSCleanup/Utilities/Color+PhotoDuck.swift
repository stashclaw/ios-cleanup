import SwiftUI

extension Color {
    // Compatibility aliases. Asset-catalog colors are the single source of truth
    // and include dark-mode variants.
    static let photoduckPrimaryPink = Color.duckPink
    static let photoduckSoftPink = Color.duckSoftPink
    static let photoduckBlushBackground = Color.duckBlush
    static let photoduckDuckYellow = Color.duckYellow
    static let photoduckBeakOrange = Color.duckOrange
    static let photoduckCream = Color.duckCream
    static let photoduckRoseText = Color.duckRose
    static let photoduckDeepBerry = Color.duckBerry
}
